[project]
name = "veni"
version = "0.1.0"

dependencies=[
    "nerfstudio>=0.3.0",
    "einops",
    "pyexr",
    "openexr",
    "wget",
    "roma",
]

[tool.setuptools.packages.find]
include = ["veni*"]

[project.entry-points.'nerfstudio.method_configs']
veni = 'veni.configs.veni_config:VENI'
